 import 'package:flutter/material.dart';
class SCANNN extends StatelessWidget {
  const SCANNN({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}